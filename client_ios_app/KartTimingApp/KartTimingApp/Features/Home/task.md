# Tasks – Pannello Notifiche Campanella

- [x] Creare `AppNotification.swift` (modello con ID stabile per UserDefaults)
- [/] Creare `NotificationsPanelView.swift` (sheet con lista, stato vuoto, tap → mark read)
- [ ] Modificare `UserHomeViewModel.swift` (calcolo notifiche + markRead con UserDefaults)
- [ ] Modificare `UserHomeView.swift` (collegare bottone campanella + badge contatore)
